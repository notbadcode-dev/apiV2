---
name: ESLint analyzer

# yamllint disable-line rule:truthy
on:
    push:
        branches: [master]

jobs:
    run-lint:
        runs-on: ${{ matrix.os }}
        strategy:
            matrix:
                os: [ubuntu-latest]
                node-version: [16.17.0]
        steps:
            - uses: actions/checkout@v3
            - name: Cache node modules
              uses: actions/cache@v3
              with:
                  path: ~/.npm
                  key: ${{ runner.os }}-node-${{
                      hashFiles('**/package-lock.json')
                      }}
                  restore-keys: |
                      ${{ runner.os }}-node-
            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v3
              with:
                  node-version: ${{ matrix.node-version }}
            - run: npm install
            - run: npm run lint > eslint-report.html 2>&1 || true
            - run: |
                  sed 's/src\//\nFile: src\//g' eslint-report.html > eslint-report-temp.html
                  sed -i 's/  \([0-9]\+\):\([0-9]\+\)/  Line \1, Column \2:/g' eslint-report-temp.html
                  sed -i 's/error \([^ ]\+\) \([^\n]\+\)/Rule: \1\n\2/g' eslint-report-temp.html
                  echo "<html><body><pre>" > eslint-report.html
                  cat eslint-report-temp.html >> eslint-report.html
                  echo "</pre></body></html>" >> eslint-report.html
                  rm eslint-report-temp.html
            - uses: actions/upload-artifact@v3
              with:
                  name: eslint-report
                  path: eslint-report.html

    send-email:
        runs-on: ubuntu-latest
        needs: run-lint
        if: ${{ needs.run-lint.result == 'success' }}
        steps:
            - name: Download ESLint Report
              uses: actions/download-artifact@v3
              with:
                  name: eslint-results
            - name: Send result lint email
              uses: dawidd6/action-send-mail@v3
              with:
                  server_address: smtp.gmail.com
                  server_port: 587
                  username: ${{ secrets.EMAIL_USERNAME }}
                  password: ${{ secrets.EMAIL_PASSWORD }}
                  subject: 'Lint Results'
                  body: 'The ESLint results are attached.'
                  from: ${{ secrets.EMAIL_USERNAME }}
                  to: ${{ secrets.EMAIL_USERNAME }}
                  attachments: |
                      ./eslint_report.html
